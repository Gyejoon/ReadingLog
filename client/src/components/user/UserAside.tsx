import React from 'react';
import styled from 'styled-components';

const UserAsideBlock = styled.aside``;

interface UserAsideProps {}

const UserAside: React.SFC<UserAsideProps> = () => {
  return <UserAsideBlock></UserAsideBlock>;
};

export default UserAside;
